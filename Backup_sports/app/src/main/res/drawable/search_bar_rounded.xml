<!-- res/drawable/search_bar_rounded.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="12dp"/> <!-- Set the corner radius to make it rounded -->
    <solid android:color="@color/white"/> <!-- Background color of the SearchView -->
    <stroke
        android:width="1dp"
        android:color="@color/gray"/> <!-- Optional border color -->
</shape>
